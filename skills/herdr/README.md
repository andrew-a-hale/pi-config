# herdr

Skills for driving the herdr agent workspace manager.

- **[parallel](./parallel/SKILL.md)** — Run an independent set of tickets in parallel
  (pi, claude, or opencode), each in its own worktree-backed agent pane, dispatching
  asynchronously and surfacing any agent that needs user input.
