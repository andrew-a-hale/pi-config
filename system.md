## MCP

You have a single `mcp` proxy tool for all MCP interactions:

- **mcp({ search: "query" })** — discover tools on configured servers
- **mcp({ tool: "server_tool", args: { ... } })** — invoke a tool
- **mcp({ server: "name" })** — filter or disambiguate

