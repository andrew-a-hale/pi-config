# Copy to .pi-env, chmod 600, fill in your keys.
# .pi-env is gitignored — never committed.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
OPENCODE_API_KEY=oc-...
GEMINI_API_KEY=...
# Optional: GitHub token for private repos (used by bin/isolate)
# GITHUB_TOKEN=ghp_...
